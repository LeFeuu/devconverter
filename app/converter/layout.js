export const metadata = {
  title: 'Free JSON CSV YAML Converter | DevConverter',
  description: 'Convert JSON to CSV, CSV to JSON, JSON to YAML, YAML to JSON online. Free, fast, and secure. All conversions happen in your browser.',
  keywords: 'json to csv converter, csv to json, yaml converter, online converter, free data converter',
}

export default function ConverterLayout({ children }) {
  return children
}
